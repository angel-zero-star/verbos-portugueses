import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eu.angelowellens.portuguesflashcards',
  appName: 'Português Flashcards',
  webDir: 'build'
};

export default config;
